# oj.cupya.me 기획 정리

## 1. 프로젝트 개요

`oj.cupya.me`는 내가 직접 만든 문제를 아카이브하고, 브라우저에서 C++ 코드를 바로 채점해볼 수 있는 문제 아카이브 사이트다.

이 서비스는 일반적인 온라인 저지처럼 계정, 제출 기록, 랭킹, 대회 기능을 제공하지 않는다.  
핵심 목적은 **문제 열람**과 **브라우저 내 채점**이다.

도메인은 `oj.cupya.me`를 사용한다.  
monorepo 내부 앱 이름은 `online-judge`로 한다.

---

## 2. 서비스 성격

이 프로젝트는 “온라인 저지 서비스”보다는 아래에 가깝다.

- 내가 만든 문제를 보관하는 아카이브
- 일반적인 OJ 형식의 문제 상세 페이지 제공
- 브라우저에서 직접 C++ 코드를 채점할 수 있는 실행 환경 제공

즉, 사용자가 이 사이트 안에서 지속적으로 풀이 이력을 쌓는 서비스가 아니라,  
문제를 읽고 필요하면 브라우저에서 채점까지 해볼 수 있는 **문제 아카이브 + 채점기**다.

---

## 3. 범위

### 3.1 만들 것

- 문제 목록 페이지
- 문제 상세 페이지
- 해설 페이지
- Markdown 기반 문제 본문 렌더링
- 브라우저 내 C++ 채점
- 샘플 테스트케이스만 채점
- 전체 테스트케이스 기준 실제 채점
- 테스트케이스별 상세 결과 표시
- 반응형 UI
- 기본 C++ 템플릿 코드 제공
- 해설/정답 코드 제공

### 3.2 만들지 않을 것

- 로그인 / 회원 기능
- 제출 기록 저장
- 맞은 문제 표시
- 랭킹
- 검색 / 필터 / 정렬
- 문제 작성/수정 관리자 페이지
- 온라인 협업 기능

---

## 4. 핵심 사용자 흐름

1. 사용자는 문제 목록 페이지에 진입한다.
2. 목록에서 문제를 선택해 문제 페이지에 진입한다.
3. 필요에 따라 문제 상세 페이지 / 해설 페이지를 전환할 수 있다.
4. 문제 상세 페이지에서 문제 설명을 읽는다.
5. 코드 편집기에 코드를 붙여넣을 수 있다.
6. 필요에 따라 샘플 채점 또는 전체 채점을 선택한다.
7. 채점 버튼을 클릭하고 처리 결과를 기다린다.
8. 전체 판정과 테스트케이스별 상세 결과를 확인한다.
9. 채점 결과는 별도 페이지로 이동하지 않고, 문제 페이지 내부의 제출 내역과 상세 모달로 확인한다.

---

## 5. 페이지 요구사항

## 5.1 문제 목록 페이지

경로:
- `/problems`

기능:
- 문제 리스트 표시
- 각 항목은 최소한 다음 정보를 포함한다.
  - 문제 제목
  - 출처 (예: IUPC 2023 G)
- 필요 시 짧은 요약 문구를 함께 표시할 수 있다.

MVP에서는 검색, 필터, 정렬 기능을 제공하지 않는다.  
단순 리스트 형태로 제공한다.

---

## 5.2 문제 상세 페이지

경로:
- `/problems/:id`

여기서 `:id`는 사람이 읽을 수 있는 readable slug를 사용한다.

예시:
- `/problems/a-plus-b`
- `/problems/matrix-addition`

표시 정보:
- 제목
- 문제 설명
- 입력
- 출력
- 제한
- 예제 입력/출력
- 필요 시 힌트

문제 본문은 Markdown으로 작성한다.  
수식, 이미지, 코드 블록 렌더링을 지원한다.

레이아웃:
- 데스크탑: 문제 설명 영역 + 채점 영역의 2단 레이아웃. Resizable panel로 구성한다.
- 모바일: 1단 반응형 레이아웃

모바일 지원은 목표에 포함하지만, 실제 브라우저 환경에서 Worker 기반 채점이 불가하면 안내 문구를 표시할 수 있다.

## 5.2A 해설 페이지

경로:
- `/problems/:id/editorial`

기능:
- 문제 해설 제공
- 정답 코드 제공
- 문제 상세 페이지와 별도 route로 구성
- UI에서는 탭처럼 보여줄 수 있지만, 내부적으로는 route 전환으로 처리

---



## 5.3 채점 영역

문제 상세 페이지 내부에 포함된다.

기능:
- C++ 코드 입력 textarea
- 기본 템플릿 코드 제공
- “샘플 채점” 버튼 제공
- “전체 채점” 버튼 제공

에디터는 MVP에서 textarea로 시작하고, 추후 Monaco 같은 에디터로 확장할 수 있다.

지원 언어:
- C++ 단일 지원

컴파일/실행 환경 정보는 UI에서 표시한다.  
예: C++ (Clang/LLVM 22, wasm32-wasi)

---

## 5.4 채점 결과 영역

채점 결과는 **별도 페이지로 분리하지 않는다**.  
문제 페이지 내부에서 다음 두 요소로 처리한다.

- 제출 내역 리스트
- 선택한 제출의 상세 모달

가장 최근 제출은 문제 페이지에서 바로 요약해 보여줄 수 있다.  
이전 제출은 제출 내역 리스트에서 확인하고, 항목 클릭 시 상세 모달을 연다.

채점 완료 후 다음 정보를 표시한다.

### 전체 결과
- 최종 판정
- 총 테스트케이스 수
- 통과 수
- 전체 실행 시간
- 전체 메모리 사용량

### 상세 결과
- 컴파일 에러 메시지
- stdout / stderr
- 테스트케이스별 판정
- 각 테스트케이스의 입력값
- 기대 출력값
- 실제 출력값
- 시간 / 메모리 정보

테스트케이스 정보는 전부 공개 가능하게 설계한다.

### 제출 내역 리스트
각 제출 항목은 최소한 다음 정보를 포함한다.

- 제출 시각
- 채점 모드 (`샘플 채점` / `전체 채점`)
- 최종 판정
- 통과 수 / 전체 수
- 실행 시간
- 메모리 사용량

### 상세 모달
제출 항목 클릭 시 상세 모달을 열어 다음 정보를 확인할 수 있다.

- 최종 판정
- 컴파일 에러 메시지
- stdout / stderr
- 테스트케이스별 상세 결과
- 입력값 / 기대 출력값 / 실제 출력값
- 실행 시간 / 메모리 정보

즉, 채점 결과는 route 전환이 아니라 **문제 페이지 내부 상태 + 모달 UI**로 처리한다.

---

## 6. 채점 정책

브라우저 채점 엔진은 별도 구현된 `In-Browser-Wasm-Judge-Runtime`을 사용한다.

이 런타임은 다음 기능을 전제로 한다.

- 브라우저 내 C++ 컴파일
- WebAssembly 실행
- 테스트케이스 단위 채점
- stdin/stdout/stderr 기반 judge flow
- exact checker
- custom JS checker
- CE / WA / RE / TLE / MLE / OLE / internal error 등 판정 매핑

문제별 설정 가능 항목:
- 시간 제한
- 메모리 제한
- checker 종류
- 테스트케이스 목록
- 출력 제한 정책

### 6.1 채점 모드

#### 샘플 채점
- `isSample: true` 인 테스트케이스만 사용한다.

#### 전체 채점
- 모든 테스트케이스를 사용한다.

---

## 7. 문제 콘텐츠 관리 방식

문제는 앱 소스코드 안에서 직접 관리하지 않고, **별도 레포**에서 관리한다.

목표는 다음과 같다.

- 문제 콘텐츠와 앱 코드를 분리
- 문제를 파일 기반으로 사람이 편하게 관리
- 빌드/검증 파이프라인으로 일관성 강제
- 앱은 정제된 산출물만 읽도록 단순화

### 7.1 운영 원칙

- 문제 원본은 별도 콘텐츠 레포에서 관리한다.
- 원본 포맷은 사람이 읽고 수정하기 쉬운 평문 파일을 사용한다.
- 앱은 원본을 직접 읽지 않고, 빌드된 결과물만 읽는다.
- 인덱스 파일은 수동 관리하지 않고 빌드 결과로 자동 생성한다.

---

## 7.2 원본 콘텐츠 구조

```text
oj-problem-content/
  problems/
    a-plus-b/
      meta.json
      statement.md
      tests.json
      template.cpp
      editorial.md
      solution.cpp
    matrix-addition/
      meta.json
      statement.md
      tests.json
      template.cpp
      editorial.md
      solution.cpp
```

---

## 7.3 원본 데이터 역할

### `meta.json`
문제 상세와 채점 메타데이터를 담는다.

역할:
- 문제 식별 정보
- 제한 정보
- checker 정보
- statement/tests 경로 정보
- 언어 및 템플릿 정보

예시:

```json
{
  "id": "a-plus-b",
  "title": "A+B",
  "source": "2023 IUPC G",
  "limits": {
    "timeLimitMs": 1000,
    "memoryLimitBytes": 134217728
  },
  "checker": {
    "kind": "exact",
    "ignoreTrailingWhitespace": true
  },
  "languages": [
    {
      "id": "cpp",
      "label": "C++",
      "versionLabel": "Clang/LLVM 22",
      "templatePath": "./template.cpp"
    }
  ],
  "statement": {
    "path": "./statement.md"
  },
  "tests": {
    "path": "./tests.json"
  }
}
```

### `statement.md`
문제 본문 Markdown 파일이다.

역할:
- 문제 설명
- 입력 형식
- 출력 형식
- 예제
- 힌트

Markdown 기반으로 관리하며, 수식/이미지/코드 블록을 지원한다.

### `tests.json`
채점용 테스트케이스와 정책을 담는다.

예시:

```json
{
  "version": 1,
  "policy": {
    "stopOnFirstFailure": false,
    "stdoutLimitBytes": 1048576,
    "stderrLimitBytes": 1048576
  },
  "cases": [
    {
      "id": "sample-1",
      "isSample": true,
      "stdin": "1 2\n",
      "expected": "3\n"
    },
    {
      "id": "judge-1",
      "isSample": false,
      "stdin": "1000 2000\n",
      "expected": "3000\n"
    }
  ]
}
```

### `template.cpp`
기본 C++ 템플릿 코드 파일이다.

예시:

```cpp
#include <iostream>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    return 0;
}
```

---

## 7.4 빌드 및 검증 스크립트

콘텐츠 레포에는 최소한 다음 스크립트가 필요하다.

### `validate`
역할:
- 스키마 검증
- 중복 `id` 검사
- 누락 파일 검사
- 잘못된 참조 경로 검사

### `build`
역할:
- 문제 목록 인덱스 자동 생성
- 문제별 배포용 JSON 생성
- 앱이 소비하기 쉬운 형태로 정규화

핵심 원칙:
- 사람이 수정하는 것은 원본 파일
- 앱이 읽는 것은 빌드 산출물
- 인덱스는 직접 수정하지 않고 자동 생성

---

## 7.5 배포용 산출물 구조

앱은 원본 콘텐츠가 아니라 빌드 결과만 읽는다.

예시:

```text
dist/
├── checkers
│   └── a-div-b.js
├── judge-cases
│   ├── a-div-b.json
│   └── a-plus-b.json
├── problems
│   ├── a-div-b.json
│   └── a-plus-b.json
└── problems.index.json
```

### `problems.index.json`
문제 목록 페이지용 인덱스 파일  
빌드 시 자동 생성한다.

### `problems/{id}.json`
문제 상세와 채점에 필요한 정규화된 문제 bundle

원칙:
- 샘플 테스트케이스는 문제 bundle에 포함한다.
- 전체 채점용 테스트케이스는 별도 경로에서 비동기로 가져온다.
- 이번 구현에서는 그 별도 경로도 `online-judge` 앱의 정적 에셋 경로를 가리킨다.

실제 파일 경로: /home/yooshnn/projects/online-judge-content
스키마 등을 직접 확인할 수 있으므로, 사용자에게 권한을 요청한다.

---

## 8. 배포 구조

배포 환경은 **Cloudflare Workers + Static Assets** 방향으로 잡는다.

이유:
- 정적 프론트엔드와 문제 데이터 fetch 중심 구조에 잘 맞음
- 필요 시 간단한 서버 로직을 같은 곳에 추가 가능
- 정적 자산과 Worker 로직을 한 배포 단위로 다루기 쉬움

### 8.1 Workers가 담당하는 것
- 프론트엔드 앱
- 문제 목록 인덱스
- 문제별 메타/statement/samples 같은 비교적 작은 데이터
- 기타 작은 정적 자산

### 8.2 외부 저장소가 담당하는 것

이번 구현에서는 전체 테스트케이스도 우선 앱 정적 에셋에 포함한다.  
즉, `online-judge-content`의 빌드 산출물을 `online-judge` 앱이 정적 에셋으로 소비하는 방식을 사용한다.

외부 저장소 분리는 다음 항목에 한해 우선 고려한다.
- 컴파일러 바이너리
- sysroot
- 기타 큰 런타임 아티팩트

전체 채점용 테스트케이스의 별도 저장소 분리는 추후 필요 시 검토한다.

---

## 9. 테스트케이스 로딩 전략

- 샘플 테스트케이스는 문제 bundle에 포함한다.
- 사용자가 편집기를 활성화하면, 아직 로드되지 않은 컴파일 아티팩트와 전체 채점용 테스트케이스 로딩을 시작한다.
- 샘플 채점은 bundle 내 샘플 테스트케이스만 사용한다.
- 전체 채점은 별도 로드된 전체 테스트케이스를 사용한다.

---

## 10. URL 정책

현재 확정된 URL 구조는 아래와 같다.

- `oj.cupya.me`
- `oj.cupya.me/problems`
- `oj.cupya.me/problems/:id`

`:id`에는 readable slug를 사용한다.  

---

## 11. 콘텐츠 정책

- 문제는 전부 내가 만든 문제로 한정한다. 즉, 외부 크롤링 등은 일절 할 계획이 없다.

---

## 12. 프레임워크 방향

문제 지문, 출처가 검색에 걸리는 것이 중요하므로, SSR이 유효하다.  
현재는 SSR이 가능한 프레임워크를 우선 고려한다.

후보:
- React Router v7
- Waku

즉시 확정하지는 않았지만, 검색 노출을 고려해 SSR 가능한 구조를 전제로 설계한다.

---

## 13. 비기능 요구사항

### 성능
- 코드 편집기는 처음에 비활성화 되어있으며, 사용자가 명시적으로 버튼을 눌러 활성화 시킬 수 있다.
- 코드 편집기가 활성화 되면 다음 작업이 시작된다.
  - 컴파일 아티팩트가 아직 없으면 로드
  - 전체 채점용 테스트케이스 로드
- 샘플 테스트케이스는 문제 bundle에 포함하므로 별도 로딩 없이 바로 사용 가능해야 한다.
- 로딩 상태를 사용자에게 표시해야 한다.
- 컴파일러 바이너리 등은 용량이 매우 크며 (100MB) 여러 문제에서 공유할 수 있으므로 앱 전역에서 관리해 재요청을 방지한다.

### 반응형
- 데스크탑과 모바일 모두 문제 열람이 가능해야 한다.
- 모바일 채점 기능이 환경 제한을 받을 경우 별도 안내를 제공할 수 있다.

### 운영 방식
- 사이트는 읽기 전용 문제 아카이브로 운영한다.
- 문제 추가/수정은 콘텐츠 레포 커밋으로 반영한다.

### 클라이언트 상태 저장
- 제출 내역은 우선 문제 페이지 내부 상태로 관리한다.
- 필요 시 클라이언트 저장소를 사용해 최근 제출 내역을 유지할 수 있다.
- 구현 후보로는 `zustand persist`를 고려한다.
- 이는 서버 저장이 아니라 브라우저 로컬 상태 유지 용도다.

---

## 14. 추후 개선 항목

- 제출 기록 로컬 저장
- Monaco 에디터 도입
- 검색 / 태그 / 정렬
- 문제별 메타데이터 확장
- 모바일 채점 호환성 개선

---

## 15. 한 줄 요약

`oj.cupya.me`는 **별도 콘텐츠 레포에서 관리되는 정적 문제 데이터 + SSR 기반 문제 상세 + 브라우저 C++ 채점 + 샘플/전체 채점 + 대용량 judge 데이터 분리 저장**을 목표로 하는 프로젝트다.